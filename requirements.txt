# FastAPI backend dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Audio / ML
numpy>=1.24
librosa>=0.10
tensorflow>=2.13          # provides keras for audio_emotion_model.h5
keras>=2.13

# Video ← audio extraction is done via the system ffmpeg binary
#   Install ffmpeg separately: https://ffmpeg.org/download.html
#   Windows quick install: winget install Gyan.FFmpeg
